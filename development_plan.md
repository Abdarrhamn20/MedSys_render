# خطة تطوير نظام IVS — استناداً لمميزات منظومة "تمام"

> ملاحظات عامة:
> - **مستبعد حالياً**: شركات التعاقد/التأمين، تعدد العيادات والتراخيص.
> - نمط التطوير الموحد: `Model` جديد → Migration → Controller + DTOs → صفحة Angular + Service → مسار + قائمة، مع ربط (hooks) بالموديولات الموجودة لتوليد القيود والسندات تلقائياً.

---

## 1) ملخص الأولويات

**P0 — أساسي**
1. نظام محاسبي: شجرة حسابات + قيود يدوية + كشف حساب + ترحيل قيد.
2. خزائن وسندات: خزائن متعددة + قبض/صرف/تحويل + ترقيم تلقائي + مذنيه يومية.
3. تحصيل من العيادة: ربط السند بحجز OPD.
4. ~~ضوابط مالية: منع تعديل المسدّد + إقفال شهري + Audit Log.~~ ✅ مكتملة: منع تعديل/دفع الفاتورة المدفوعة + الإقفال المالي الشهري (FiscalClosedThrough) يمنع التحصيل (بطاقة/نقد) والترحيل/العكس في الفترات المقفلة + سجل تدقيق (AuditLog) بصفحة وفلترة (actionType/entityType/بحث/تاريخ) — اختبار حي 39/39.

**P1 — توسعة تشغيلية**
5. ~~مخزن متكامل: مخازن متعددة + أصناف بشجرة + سندات إدخال/إخراج/تحويل + حركة وكميات.~~ ✅ مكتملة (نماذج + ترحيل + Controller + صفحة Angular + اختبار حي 54/54)
   - **توسعة**: جرد دوري (StockCount + تسوية أرصدة تلقائية + عكس) + تصدير Excel/CSV (أرصدة/حركة/أصناف/جرد) — اختبار حي 58/58.
6. ~~موظفون: بطاقة موظف + دورات + إجازات + رواتب.~~ ✅ مكتملة: بطاقات موظفين (رقم `EMP-YYYY-NNNN` + نموذج تعويض FixedSalary/Commission/Mixed + ربط/إنشاء حساب دخول) + دورات تدريبية + إجازات (طلب/اعتماد/رفض + خدمة ذاتية `me`) + رواتب شهرية (توليد مسودات بأساسي + عمولات الأطباء من الفواتير المدفوعة + مكافأة/خصم + ترحيل قيد 5010/2020 تلقائياً + عكس) محمية بالإقفال المالي + صفحة موارد بشرية — اختبار حي 39/39.
7. مهام + عملاء/موردون ككيانات مستقلة.

**P2 — تخصصية**
8. ~~مختبر متقدم: تحاليل مركبة + مزرعة وحساسية + ربط أجهزة.~~ ✅ مكتملة: طلبات متعددة العناصر مع فكّ البانلات تلقائياً + مزرعة/حساسية (CultureSensitivity + SensitivityResult) + أجهزة (LabDevice + التقاط نتيجة) + احتساب Normal/High/Low آلياً (جنس/عمر) + إكمال الطلب تلقائياً + توافق خلفي `LabTestID` — اختبار حي 20/20.
9. ~~ملفات مرضى محسّنة: التركيبة الاسمية الليبية + دمج ملفين + رقم ملف تسلسلي.~~ ✅ مكتملة: `FirstName/FatherName/GrandfatherName/FamilyName` + `FileNumber` بتنسيق `PT-YYYY-NNNN` (توليد تلقائي + backfill للموجودين) + دمج ملفين (نقل السجلات بـ PatientID/PatientUserID + تعطيل المصدر + Audit) + صفحة ملفات مستقلة — اختبار حي 11/11.
10. مراكز تخصص: جدولة جلسات كلى + مطبخ المنومين.
11. ~~صلاحيات أدوار جديدة (محاسب، أمين مخزن، كاشير).~~ ✅ مكتملة (محاسب + كاشير): `AssignedTreasuryID` للكاشير (UsersController) + حماية TreasuryController/AccountingController حسب الدور (403) + إخفاء شاشات حسب الدور في الواجهة + اختبار حي 41/41.

**لمسات احترافية**
12. تصدير PDF/Excel لكل الاستعلامات.
13. إشعارات SignalR فورية.
14. Soft Delete + أرشفة + اختبارات.
15. معيار القبول: دورة مالية كاملة دون تدخل يدوي.

---

## 2) الوظائف الإضافية من تمام (غير المُغطاة سابقاً)

1. فواتير المشتريات ومرتجعاتها (من الموردين).
2. استعلام المبيعات ورصيد ديون العملاء.
3. استعلام خدمات العيادات (إيراد كل خدمة).
4. ربط الأطباء بالتخصصات/المستخدمين.
5. وصفة/عرض النظارات.
6. مزوّدو الخدمات الآخرون (مختبرات مرجعية).
7. سجل الحركات (TranLog) → يُدمج في Audit Log.

---

## 3) توزيع الصلاحيات على الأدوار (الملخص)

| الدور | الإدارة | المحاسبة | الخزائن | المخزن | المرضى | المختبر | الأشعة | الصيدلية | التنويم |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| مدير | كامل | كامل | كامل | كامل | كامل | كامل | كامل | كامل | كامل |
| محاسب | – | كامل | كامل | – | – | – | – | – | – |
| أمين مخزن | – | – | – | كامل | – | – | – | جزئي (دواء) | – |
| كاشير | – | – | خزينته | – | – | – | – | – | – |
| استقبال | – | – | تحصيل فقط | – | إنشاء/بحث | – | – | – | – |
| طبيب | – | – | تحصيل كشفه | – | ملفات مرضاه | طلب/نتيجة | طلب/نتيجة | طلب | متابعة |
| صيدلاني | – | – | – | أصناف دواء | – | – | – | كامل | – |
| مختبر/أشعة | – | – | – | – | – | محطة | محطة | – | – |
| تمريض | – | – | – | استهلاك | – | – | – | – | تنفيذ أوامر |
| مريض | – | – | – | – | سجلاته فقط | نتائجه | تقاريره | – | سجلاته |

**قاعدة التنفيذ**: إخفاء الشاشات حسب الدور في الواجهة (layout.html + app.routes.js roles) + فحص الدور في كل Controller Action (خاصة التحصيل والسندات).

---

## 4) توضيح الموازنة بين "الرواتب" ونظام الأطباء الحالي

النظام الحالي يحسب **أرباح الأطباء من العمليات** (سعر كشف لكل طبيب + نسبة من الكشف للطبيب/للعيادة أو سعر ثابت للكشف)، بينما فكرة "الرواتب" **شهرية ثابتة**. لا تعارض بينهما — الحل: تعريف **"نموذج تعويض" (Compensation Model)** لكل موظف/طبيب.

### نماذج التعويض الأربعة
| النموذج | من يعمل به | المصدر | المعالجة |
|---|---|---|---|
| نسبة من الكشف (مثلاً 70% للطبيب) | طبيب | المحرك الحالي (Commissions) | كل كشف يخصّم نسبته تلقائياً ويراكم في كشف الطبيب |
| سعر ثابت للكشف | طبيب | المحرك الحالي | مبلغ ثابت لكل كشف يتراكم في كشف الطبيب |
| راتب شهري ثابت | موظفون إداريون + أطباء براتب | وحدة الرواتب الجديدة | مبلغ شهري ثابت مستقل عن عدد الكشوفات |
| مختلط (راتب + نسبة) | طبيب | الاثنان معاً | راتب أساسي + نسبة من الكشوفات فوقه |

### قواعد الموازنة (منع الازدواج)
1. حقل `CompensationModel` يُضاف لبروفايل الطبيب والموظف ويحدد أحد النماذج الأربعة.
2. المحرك الموجود (سعر الكشف + توزيع النسبة/السعر الثابت) **يبقى كما هو** في الفوترة.
3. وحدة الرواتب الجديدة **لا تستبدل العمولات** بل تضيف بُعداً شهرياً:
   - طبيب بنسبة/سعر ثابت → أرباحه من العمولات، ووحدة الرواتب فارغة له.
   - موظف إداري → لا عمولات، راتبه شهري ثابت.
   - طبيب مختلط → راتب أساسي + نسبة من الكشوفات.
4. **قاعدة صارمة**:
   - إذا كان الطبيب "راتب شهري ثابت" → يتوقف حساب النسبة له تلقائياً (يحسم سعر كشفه للعيادة).
   - إذا كان "نسبة/سعر ثابت" → لا يُصرف له راتب شهري إلا إذا صُنّف "مختلط" صراحة.
   - النظام يرفض حفظ تناقض في النموذج.

### المعالجة المحاسبية
- **العمولات**: حساب "مصاريف عمولات أطباء" — يُقيَّد من كل كشف عند السداد (مستحق للطبيب).
- **الرواتب**: حساب "مصاريف رواتب" — يُقيَّد شهرياً بقيد: *رواتب مدين / ذمم رواتب دائن*، وعند الصرف: *ذمم رواتب مدين / خزينة دائن*.
- حسابان منفصلان لتفريق العمولة عن الراتب في التقارير.

### التسوية الشهرية (كشف الأرباح الموحّد)
في نهاية الشهر تُشغّل "تسوية الرواتب" وتنتج:
- طبيب بنسبة: مجمـوع العمولات المحصّلة ← مستحقه.
- موظف براتب: مبلغ الراتب الثابت.
- مختلط: الراتب + العمولات.
- الناتج الكلي يظهر في **DoctorLedger المطوّر** (راتب + عمولات) ويُنشئ قيداً واحداً في النظام المحاسبي.

---

## 5) تفصيل ربط أجهزة المختبر

الهدف: استيراد نتائج التحاليل من الأجهزة مباشرة بدل الإدخال اليدوي (تقليل الأخطاء وتسريع العمل)، مع بقاء الإدخال اليدوي كخيار احتياطي.

### أ. نموذج البيانات
- `LabMachine`: تعريف الجهاز (الاسم، الطراز، نوع الاتصال، البروتوكول).
- `LabMachineTestMapping`: ربط نتيجة من الجهاز بتحليل محدد في النظام (`LabTest`).
- `LabMachineImportLog`: سجل كل عملية استيراد (مصدر، عدد النتائج، الأخطاء، المستخدم، الحالة).

### ب. طرق الربط المدعومة (تدريجياً)
| الطريقة | الوصف | الملاءمة |
|---|---|---|
| استيراد ملف CSV/XML | رفع ملف نتائج من الجهاز وربطها بالأوامر | أبسط وأسرع (المرحلة الأولى) |
| ربط مباشر (فصل/COM/شبكة) | قراءة مباشرة من منفذ الجهاز | للأجهزة المحلية المتصلة بالشبكة |
| بروتوكول HL7/LIS | المعيار العالمي لتبادل نتائج المختبرات | للمستشفيات والأنظمة الكبيرة |

### ج. سير العمل
1. **إعداد الجهاز** (مدير): تعريف الجهاز + تحديد نوع الاتصال + تكوين خريطة النتائج (أي عمود في الملف يطابق أي `LabTest`).
2. **الاستيراد** (فني مختبر): رفع/جلب النتائج → يقوم النظام بتحويلها إلى أداة تأكيد.
3. **المطابقة**: ربط كل نتيجة بأمر المختبر (`LabOrder`) الموجود ورقم المريض — من رقم الطلب/الباركود.
4. **المراجعة**: الفني يستعرض النتائج المستوردة، يعالج ما لم يُطابق، ثم **يعتمدها** فتصبح النتيجة النهائية للطلب.
5. **التحقق التلقائي**: مقارنة كل نتيجة بالنطاق المرجعي (`LabReferenceRange`) الموجود → وضع علامة "غير طبيعي" وتلوين الشاذ تلقائياً.

### د. الشاشات والأدوار
| الشاشة | مدير | فني مختبر |
|---|---|---|
| إعداد الأجهزة + خريطة النتائج (Mapping) | ✅ | – |
| استيراد النتائج | ✅ | ✅ |
| مراجعة/اعتماد النتائج المستوردة | – | ✅ |
| سجل الاستيراد (Imports Log) | ✅ | ✅ |

### هـ. اللمسات الاحترافية
- كشف التكرار: رفض/تنبيه عند استيراد نتائج لطلب معتمد مسبقاً.
- سجل تدقيق لكل استيراد (من استورد، متى، كم نتيجة، أي أخطاء).
- إشعار SignalR للطبيب عند اعتماد نتائج طلبه.
- وضع "دون اتصال" لاستيراد الملفات يدوياً عند تعذر الربط المباشر.

---

## 6) معايير القبول النهائية
- دورة مالية كاملة: حجز → كشف → تحصيل بسند → قيد تلقائي → كشف حساب → مذنيه، دون تدخل يدوي.
- لا ازدواج بين عمولات ورواتب أي موظف.
- كل شاشة محمية بفحص الدور على الخادم، وكل عملية مالية مسجلة في Audit Log.
